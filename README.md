# File Tree

This is a project I am making for a greater project.

## How To Use

If you are stupid enough to use this, here is how to.

Put a `<file-tree></file-tree>` inside of the HTML.

Next grab the `element` and use `element.paths` to add paths for the tree.
